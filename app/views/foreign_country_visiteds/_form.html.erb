<%= form_for(@foreign_country_visited) do |f| %>
  <% if @foreign_country_visited.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@foreign_country_visited.errors.count, "error") %> prohibited this foreign_country_visited from being saved:</h2>

      <ul>
      <% @foreign_country_visited.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.hidden_field :personal_detail_id, value: @personal_detail.id %>

  <div class="field">
    <%= f.label :start_date %><br />
    <%= f.date_select :start_date, start_year: Date.today.strftime("%Y").to_i - 70, end_year: Date.today.strftime("%Y").to_i %>
  </div>
  <div class="field">
    <%= f.label :end_date %><br />
    <%= f.date_select :end_date, start_year: Date.today.strftime("%Y").to_i - 70, end_year: Date.today.strftime("%Y").to_i %>
  </div>
  <div class="field">
    <%= f.label :country_visited %><br />
    <%= f.text_field :country_visited %>
  </div>
  <div class="field">
    <%= f.label :purpose_of_visit %><br />
    <%= f.text_field :purpose_of_visit %>
  </div>
  <div class="actions">
    <%= f.submit "Save", class: "btn" %>
  </div>
<% end %>
